#include<iostream>
#include <fstream>
int main () {
  while(words >> x) {
    std::ifstream words;
    std::string x;
    words.open(argv[0]);
    std::cout << x << "is trying to be added";
    myTrie.addAWord(x);
  }
}
